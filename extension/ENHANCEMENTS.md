# Future Enhancements

- [ ] **Icons** — `icons/` folder is empty; Chrome shows a default puzzle piece. Add 16/48/128px PNGs before publishing to Chrome Web Store.
- [ ] **Configurable hover delay** — expose the 2-second delay as a slider in the popup instead of hardcoded in `content.js`.
- [ ] **README: add timer lesson** — document the `showTimer`/`hideTimer` split (why a single timer variable causes show/hide to cancel each other when moving between child elements of the same avatar).
